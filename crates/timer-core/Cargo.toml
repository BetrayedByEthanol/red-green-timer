[package]
name = "timer-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
thiserror = "1"
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["serde"] }
