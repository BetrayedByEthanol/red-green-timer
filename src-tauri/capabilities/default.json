{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability granting the main window access to the app's own IPC commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
